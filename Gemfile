source 'https://rubygems.org'

# Specify your gem's dependencies in active_bootstrap_skin.gemspec
gemspec

# Specify more restricted dependencies (for development)
gem 'bootstrap-sass',
    git:    'https://github.com/emjot/bootstrap-sass',
    branch: 'remove-sassc'

# dev dependencies
gem "bundler", "~> 2.3"
gem "rake", "~> 13.0"
